# BACKLOG — Гибкие напоминания

Список задач с приоритетами. Обновляется по мере выполнения.

## Принципы приоритизации

1. По сложности — простое вперёд.
2. По важности для пользователя — важное раньше.
3. По количеству переделок — группируем так, чтобы модель данных менялась минимум раз.
4. Учитываем публикацию в RuStore: минимум разрешений, бесплатное приложение.

---

## ✅ Готово

### Инфраструктура
- [x] Проект на Kotlin + Jetpack Compose + Room
- [x] GitHub-репозиторий: https://github.com/Egor-Che/FlexReminder
- [x] GitHub Actions сборка APK
- [x] Постоянный ключ подписи → обновления без конфликтов
- [x] Gradle wrapper в репозитории

### Функциональность
- [x] Создание/редактирование/удаление напоминаний
- [x] Режим «Интервалы» (N дней вкл / M дней выкл)
- [x] Режим «Конкретные даты» (ручной выбор в календаре, множественный выбор)
- [x] Синхронизация режимов: интервалы ↔ календарь, распознавание паттернов
- [x] Период [startDate; endDate] с окончанием срабатываний по концу периода
- [x] Точные будильники через AlarmManager (setExactAndAllowWhileIdle)
- [x] Уведомления с кнопками «Отложить 5 мин» / «Отложить 10 мин»
- [x] Беззвучный режим через отдельный notification channel
- [x] Экран просмотра с календарём-подсветкой ON-дней
- [x] Восстановление расписания после перезагрузки (BootReceiver)
- [x] Авто-перепланирование при запуске приложения

---

## 🌊 Волна 0 — Быстрые победы

**Время:** ~1 вечер. Модель данных почти не трогаем.

- [ ] **Отмена отложенного snooze при выключении события**
  - Если пользователь нажал «Отложить 5 мин», а потом выключил/удалил событие — убить отложенный будильник.
  - Хранить `snoozeId` в Reminder или отдельный список pending snooze.
  - Правки: `SnoozeReceiver.kt`, `AlarmScheduler.kt`, возможно `Reminder.kt` (+1 поле).

---

## 🌊 Волна 1 — Статусы, история, цвета (одна миграция БД → v4)

**Время:** 3–4 вечера. Всё делаем сразу, чтобы миграция была одна.

- [ ] **Статус «Выполнено»**
  - Поле `lastCompletedAt: Long?` в Reminder.
  - Кнопка в уведомлении (action), в карточке списка, на экране просмотра.
  - Отметка снимает напоминание с сегодня и фиксирует факт.
- [ ] **Статус «Пропущено»**
  - Поле `lastSkippedAt: Long?` в Reminder.
  - Автоматом: если время срабатывания прошло, а реакции нет в течение N часов.
  - Визуальная метка в списке/просмотре.
- [ ] **История срабатываний**
  - Новая таблица `history` (id, reminderId, plannedAt, firedAt, status, note).
  - Экран «История» с возможностью удаления записей и очистки всей истории.
- [ ] **Цветовая гамма (16 пастельных оттенков)**
  - Enum `ReminderColor` с 16 hex-значениями.
  - Поле `colorIndex: Int` в Reminder.
  - Применяется: светлый тон — фон карточки в списке, тёмный — свитч, акценты, подсветка календаря.
  - **Требования к палитре:**
    - Каждый RGB-канал в диапазоне **[64; 191]** (отступ ≥25% от краёв 0 и 255).
    - Пастельные, ненасыщенные тона.
    - Исключаем: чёрный, белый, серый, коричневый.
  - Примеры подходящих оттенков (для ориентира): лавандовый, мятный, персиковый, пудрово-розовый, небесно-голубой, оливковый, сиреневый, бирюзовый, коралловый, сливовый, песочный, мятно-зелёный, лазурный, розово-лиловый, фисташковый, дымчато-синий.
- [ ] **Улучшение карточки списка**
  - Показ последнего статуса («Выполнено вчера», «Пропущено 2 дня назад»).
  - Быстрые действия прямо в карточке.

---

## 🌊 Волна 2 — Свои звуки


**Время:** ~2 вечера.

- [ ] **Выбор звука уведомления**
  - Системные звуки — через RingtoneManager.
  - Свой файл — через SAF (`ACTION_OPEN_DOCUMENT`, тип `audio/*`). **Разрешения не нужны.**
  - Хранить в Reminder: `soundUri: String?` (null = системный по умолчанию).
  - Кастомный звук на уведомление — через новый notification channel с уникальным ID для каждого URI.
  - ВАЖНО: кастомные URI персистентны только через `takePersistableUriPermission`.

**Кастомный звук НЕ переносится между устройствами и НЕ шарится через мессенджеры.**
При импорте/приёме напоминания поле `soundUri` сбрасывается, звук — системный по умолчанию.
Причина: чтобы не запрашивать разрешения на файлы и не раздувать APK/JSON.

---

## 🌊 Волна 3 — Перенос и шаринг (общий механизм)

**Время:** ~2 вечера.

- [ ] **Экспорт/импорт всех данных**
  - Формат: JSON, расширение **`.flexreminder`** (НЕ `.json` — чтобы Android предлагал только наше приложение, а не сотню разных).
  - MIME-тип: `application/x-flexreminder`.
  - Что переносится: напоминания (все поля кроме `soundUri`), история (опционально галочкой), цвет.
  - Что НЕ переносится: `soundUri` (кастомный звук).
- [ ] **Поделиться напоминанием / применить у другого пользователя**
  - Тот же JSON, отправка через `Intent.ACTION_SEND` в мессенджер.
  - **Intent-filter** в манифесте:
    ```````xml
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="content" />
        <data android:scheme="file" />
        <data android:mimeType="application/x-flexreminder" />
        <data android:host="*" />
        <data android:pathPattern=".*\\.flexreminder" />
    </intent-filter>
При импорте: предпросмотр + кнопка «Сохранить». Данные попадают как обычное напоминание, доступное для редактирования и удаления.
---

## 🌊 Волна 4 — Продвинутое планирование

**Время:** ~4–6 вечеров.

- [ ] Несколько срабатываний в один день (разное время).
- [ ] Разное время внутри цикла (например, по будням в 8:00, по выходным в 10:00).
- [ ] Пропуск конкретных дат (праздники, отпуск).
- [ ] Свайп для удаления в списке.
- [ ] Сортировка, поиск, группировка по категориям.

---

## 🌊 Волна 5 — Виджет

**Время:** ~2–3 вечера.

- [ ] Виджет на рабочий стол с ближайшими напоминаниями.
- [ ] Поддержка разных размеров.
- [ ] Синхронизация с БД при изменениях.

---

## 🌊 Волна 6 — Полировка и публикация

**Время:** ~3–5 вечеров.

- [ ] Красивый UI: иконка приложения, типографика, отступы, анимации переходов.
- [ ] Материалы для RuStore: скриншоты, иконка, обложка, видео.
- [ ] Описание приложения: без кликбейта, с преимуществами над другими напоминалками.
- [ ] README на GitHub.
- [ ] Changelog.
- [ ] Финальное тестирование.

---

## Разрешения

**Оставляем:**
- `POST_NOTIFICATIONS` (Android 13+) — обязательно.
- `SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM` — для точных будильников.
- `RECEIVE_BOOT_COMPLETED` — восстановление после перезагрузки.
- `WAKE_LOCK` — не критично, можно убрать.

**Не нужно (и это преимущество):**
- Интернет, камера, контакты, геолокация.
- `READ_MEDIA_AUDIO` — не нужен, работаем через SAF.

---

## Идеи «когда-нибудь» (не в плане)

- Виджет с интерактивными кнопками «Выполнено» / «Отложить».
- Напоминания по геолокации (не для RuStore — много разрешений).
- Заметки к срабатываниям («как прошло?»).
- Категории/теги.
- Синхронизация через облако пользователя (файл в Google Drive / Я.Диск).